import { StyleSheet } from 'react-native';
import { cors } from '.';

export default StyleSheet.create({
  campo: {
    height: 25,
    borderWidth: 1,
    borderColor: cors.escuro,
    textAlign: 'center',
  }
});